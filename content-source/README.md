# Content Source

Drop the real photos and copy documents GLS provides here (Word, PDF, JPG, PNG) so they're kept in the repo for reference. Files here are not linked from the live site directly — use them as the source when replacing placeholder images/text in `assets/images/` and the page HTML.

Suggested subfolders as material arrives:

```
content-source/
  photos/
  documents/
```
